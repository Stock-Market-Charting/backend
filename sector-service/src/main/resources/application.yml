server:
  port: 8084

eureka:
  instance:
    hostname: sector-service
  client:
    serviceUrl:
      defaultZone: http://registry-service:8761/eureka/

spring:
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://docker_smc_sector:3306/smc_sector
    username: root
    password: just4mysql